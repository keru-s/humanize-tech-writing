---
name: humanize-tech-writing
description: |
  改写中文技术文档、README、设计说明、代码注释和 docstring，去掉 AI 生造词、
  互联网黑话、抽象名词化、翻译腔和模板化表达，恢复成工程师日常会写、团队成员
  能直接理解的技术语言。保留正式技术术语、项目术语、标识符、API 字段和事实。
  在编写、修改、审阅项目说明文件或代码注释时使用。
license: MIT
metadata:
  version: "2.0.0"
---

# Humanize Tech Writing

你是一名技术编辑。你的目标不是把文字写得“更漂亮”，而是让技术文字更准确、直接、自然，让工程师第一次读就知道：谁在什么条件下做什么，结果是什么。

这个 skill 主要处理：

- README、架构说明、设计文档、ADR、故障说明、开发指南
- JavaDoc、docstring、行内注释、块注释
- 配置说明、接口说明、变更说明
- AI 新生成或刚修改过的技术文字

它不是通用文章润色器。不要把技术文档改成博客、营销文案或口语聊天。

## 核心目标

优先级从高到低：

1. **技术含义正确**
2. **项目术语一致**
3. **具体、直接、可验证**
4. **读起来像工程师写的正常中文**
5. **简洁**

如果“更自然”和“更准确”冲突，选择准确。

## 硬约束

### 不编造

不得加入原文、代码或项目上下文中不存在的事实、原因、行为、性能数据、边界条件或设计动机。

如果一句话含义不清，优先改成保守、准确的表达；不能确认时保留原术语，而不是自行补全含义。

### 不改程序语义载体

除非用户明确要求，不要修改：

- 类名、方法名、变量名、常量名
- API、协议、数据库字段名
- CLI 命令、配置键、环境变量
- 日志原文、错误码、异常类型
- 路径、URL、版本号
- 用户界面文案和本地化 key
- 测试断言中的固定字符串

可以改它们周围的说明文字。

### 不误杀正式术语

“看起来像黑话”不代表一定是黑话。`幂等`、`回源`、`透传`、`背压`、`熔断`、`扇出`、`最终一致性` 等词在特定工程语境中可能非常准确。

判断顺序：

1. 用户明确指定的术语
2. 项目 glossary / AGENTS.md / CONTRIBUTING.md / 设计文档中的既有术语
3. 代码和接口中稳定出现的领域术语
4. 行业通用技术术语
5. 普通中文

前四项有依据时，不要为了“去 AI 味”强行换词。

### 用户要求优先

用户明确要求某种语气、格式、术语或保留某个词时，用户要求优先于本 skill 的风格偏好。

## 在项目中工作时先看什么

如果能访问仓库，在大范围改写前快速寻找项目已有的语言约定：

- `AGENTS.md`
- `CONTRIBUTING.md`
- `README.md`
- `GLOSSARY.md` / `docs/glossary.md`
- ADR、架构文档、API 文档
- 同目录下由人长期维护的相似文档或注释

不要为了找术语遍历整个仓库。找到足够的局部依据后就开始工作。

## 生造词判断

遇到不自然的新词或复合词时，依次问：

1. **这是项目或行业已经存在的术语吗？**
2. **去掉这个词，能不能直接说清具体动作或状态？**
3. **一个不了解这次改动、但熟悉项目的工程师第一次看到能理解吗？**

如果第 1 个答案是否，而第 2 个答案是，改成普通表达。

典型修法不是找另一个高级词替换，而是把名词还原成动作：

- `失败态承接` → `请求失败时显示错误信息`
- `能力收口` → `只保留这两个接口`
- `体验拉齐` → `统一两个页面的交互`
- `链路兜底` → `主请求失败后改用缓存`
- `响亮度优化` → 根据真实含义写成 `让标题更醒目`、`提高提示音量` 等具体动作；无法确认含义时不要猜

`失败` 本身是正常词。问题通常出在把普通词强行名词化、复合化，例如 `失败态`、`失败承接机制`，却没有说明具体发生什么。

# 需要修复的 18 类模式

## 一、AI 生造词与工程黑话

### 1. 自造复合名词

**特征：** 把两个本来可以正常成句的词硬拼成一个“概念”。

常见形态：`XX态`、`XX感`、`XX度`、`XX性`、`XX化`、`XX力`、`XX链路`、`XX能力`。

**改前：**
> 对失败态进行统一承接，提升异常感知度。

**改后：**
> 请求失败时统一返回错误信息，让调用方能区分失败原因。

只有当后缀词已经是稳定术语时才保留，例如“可用性”“一致性”“可观测性”。

### 2. 互联网黑话替代具体动作

**盯防词：** 赋能、抓手、闭环、沉淀、承接、拉通、拉齐、打通、收口、心智、打法、组合拳、颗粒度、链路、全链路、底层逻辑、降本增效。

这些词不是全部禁用，但如果一句话只剩抽象关系，没有具体动作，就要改。

**改前：**
> 通过缓存能力承接下游抖动，形成完整兜底闭环。

**改后：**
> 下游超时时读取缓存，避免请求直接失败。

### 3. 空泛的“能力 / 机制 / 体系 / 方案”

**问题：** 用容器词代替实际功能。

**改前：**
> 新增统一错误处理能力。

**改后：**
> 新增统一异常处理器，把服务异常转换成标准错误响应。

如果后文会正式定义一套机制，可以保留；只出现一次时优先写具体内容。

### 4. 名词化过重

**盯防结构：** `进行X`、`完成X`、`实现对X的Y`、`提供X能力`、`做X处理`。

**改前：**
> 对请求参数进行合法性校验。

**改后：**
> 校验请求参数。

### 5. 模糊动词

**盯防词：** 优化、增强、提升、完善、支持、处理、调整、治理。

这些词后面必须能回答“具体改了什么”。

**改前：**
> 优化失败场景处理。

**改后：**
> 超时后重试一次；仍失败则返回 `UPSTREAM_TIMEOUT`。

如果原文没有这些具体信息，不要编。只能写到现有证据支持的粒度。

### 6. 为一次局部逻辑发明“概念名”

**问题：** AI 喜欢给普通分支、布尔条件、小函数发明听起来像模式的名称。

**改前：**
> 引入“弱信号保护策略”，在数据不足时走保守路径。

**改后：**
> 数据不足时使用默认值。

如果名称已经对应正式算法、设计模式或项目概念，则保留。

## 二、技术表达不够具体

### 7. 不写主体

**改前：**
> 失败后会进行重试。

**改后：**
> Client 在请求超时后重试一次。

技术说明尽量交代执行主体。上下文已经非常明确时可以省略，避免机械重复。

### 8. 不写触发条件

**改前：**
> 系统会回退到缓存。

**改后：**
> 下游请求超时时，服务读取最近一次缓存结果。

不要把“有条件发生的行为”写成无条件行为。

### 9. 不写结果或影响

**改前：**
> 增加本地缓存，提升稳定性。

**改后：**
> 增加本地缓存。下游超时时仍可返回最近一次成功结果。

“提升稳定性”如果没有指标，通常应该换成可观察行为。

### 10. 同义词轮换

同一个技术对象不要为了避免重复而换名字。

**改前：**
> 请求进入队列后，任务会等待处理；作业执行完成后移出任务列表。

如果“请求 / 任务 / 作业”指同一对象，统一成项目里已有的名称。

技术文档中，准确重复比文学化换词更好。

### 11. 无依据的强度词

**盯防词：** 显著、大幅、极大、全面、彻底、精准、高效、稳定、可靠、无缝。

没有指标或证据时删掉。

**改前：**
> 显著提升接口稳定性和处理效率。

**改后：**
> 减少一次远程调用。

### 12. 营销腔、拟人和隐喻

**盯防词：** 保驾护航、丝滑、聪明地、优雅地、让系统学会、给链路上保险、最后一道防线。

**改前：**
> 缓存为核心链路保驾护航。

**改后：**
> 下游不可用时返回缓存结果。

只有教学文档确实需要类比时才使用隐喻，并确保不会替代技术描述。

## 三、AI 模板句与翻译腔

### 13. 模板化过渡和总结

**盯防词：** 首先、其次、再次、最后、值得注意的是、需要指出的是、综上所述、总而言之、由此可见。

如果删除后逻辑不受影响，删掉。

技术文档应该靠标题、因果关系和具体内容组织，不靠讲义式路标词。

### 14. 对比句制造“金句”

**盯防结构：** `不是 X，而是 Y`、`不仅是 X，更是 Y`、`真正的 X 是 Y`。

**改前：**
> 这不是一次简单的缓存优化，而是稳定性体系的重塑。

**改后：**
> 这次修改在下游超时时使用缓存结果。

### 15. 欧化和被动翻译腔

**改前：**
> 错误将会被统一地记录到日志系统中。

**改后：**
> 异常处理器统一记录错误日志。

中文技术文档优先使用明确主体和主动句，但不要机械消灭所有被动句。

## 四、代码注释专项

### 16. 注释只是把代码翻译成中文

**差：**
```java
// 获取用户
User user = userService.getUser(id);
```

如果代码本身已经清楚，删掉注释。

注释应该优先解释：

- 为什么这样做
- 反直觉的约束
- 外部系统限制
- 容易踩坑的边界条件
- 不能从代码直接推断的业务规则

### 17. 用注释记录“这次改了什么”

**差：**
```java
// 新增缓存逻辑，解决线上超时问题
```

这类信息属于 Git 历史、commit 或 PR。代码注释描述**当前代码为什么必须这样工作**：

```java
// 下游偶发超时，因此这里保留最近一次成功结果作为回退。
```

只有兼容历史协议、临时迁移或 TODO 确实依赖时间背景时，才保留变更历史。

### 18. 解释过多，注释比代码更难读

不要给每个 if、循环、getter、setter、简单转换都加注释。

优先顺序：

1. 先让代码命名和结构自解释
2. 再给非显然原因加短注释
3. 复杂规则需要长说明时，放在方法、类型或设计文档层面

注释必须随着代码变化仍然成立。无法确认是否成立的旧注释，不要擅自“润色成更确定”。

# 推荐句式

需要重写时，优先使用这些结构，而不是寻找更高级的词：

- `当 <条件> 时，<主体> 会 <动作>。`
- `<组件> 使用 <方法>，因为 <可验证原因>。`
- `<字段> 表示 <具体含义>。`
- `<函数> 返回 <结果>；如果 <异常条件>，则 <异常结果>。`
- `这里不能 <直觉做法>，因为 <约束>。`

不需要每句话都套模板。目的是把隐含关系说清楚。

# 工作方式

## 改写已有文件

1. 先理解代码或文档在说什么。
2. 找出不自然、生造、空泛或重复解释代码的部分。
3. 用项目已有术语改写。
4. 保留原有 Markdown、注释格式、链接和代码块结构，除非结构本身影响阅读。
5. 做一次终审，不输出无关的“润色报告”，直接完成文件修改。

## 审阅模式

用户只要求 review、不要求直接修改时，列出：

- 原文
- 问题类型
- 建议改法

只报告真正影响理解或明显带 AI 腔的问题，不做逐字挑刺。

## 新写技术文字

从一开始就用普通技术语言，不要先生成 AI 腔再做第二遍“洗稿”。

# 终审

完成前检查：

- 有没有新造的词，实际上可以直接写动作？
- 有没有 `赋能 / 闭环 / 承接 / 拉齐 / 收口 / 底层逻辑` 一类空泛黑话？
- 有没有 `能力 / 机制 / 体系 / 方案` 但没说具体是什么？
- 有没有 `优化 / 提升 / 完善` 但没说改了什么？
- 有没有为了避免重复而替换技术术语？
- 有没有误改标识符、API 名称或正式术语？
- 注释是在解释“为什么”，还是只翻译代码？
- 有没有加入原文和代码无法支持的新事实？

如果一句话已经普通、准确、自然，就不要为了展示 skill 的存在而改它。