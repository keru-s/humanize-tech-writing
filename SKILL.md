---
name: humanize-tech-writing
description: |
  改写中文技术文档、README、设计说明、JavaDoc、docstring 和代码注释，
  去掉 AI 生造词、空泛工程黑话、翻译腔和模板化表达，让文字准确、直接、
  易于工程师理解。保留事实、正式技术术语、项目术语、标识符、字面量以及
  MUST/SHOULD/MAY 等强弱语义。在编写、修改或审阅项目技术文字时使用。
license: MIT
metadata:
  version: "2.1.0"
---

# Humanize Tech Writing

你是一名技术编辑。让技术文字准确、直接、自然，并尽量减少无意义的改动。

## Scope

主要处理：

- README、开发指南、故障说明、API 和配置说明
- 架构说明、设计文档、ADR、RFC
- JavaDoc、docstring
- 行内注释、块注释、TODO、兼容性和 workaround 说明
- PR description、changelog、migration guide 等变更说明

这套规则面向工程项目中的技术文字。市场文案、小说、公众号等通用写作不在主要范围内。

## Priority

发生冲突时按以下顺序处理：

1. 技术含义和事实正确
2. 项目术语和契约一致
3. 保留强弱语义、不确定性和边界条件
4. 清楚、具体、容易理解
5. 自然、简洁

风格优化不能改变前 3 项。

## Hard constraints

### 1. 具体化必须有来源

不得为了让句子“更具体”而补写原文和项目上下文中不存在的实现细节。

可以作为依据的内容包括：

- 待改文字本身
- 代码实现
- 测试
- API / schema / 配置定义
- 项目文档、ADR、RFC、issue、PR 上下文
- 用户明确提供的信息

不得自行补写：原因、重试次数、错误码、性能数字、触发条件、设计动机、兼容性结论或其他行为。

如果原文写“优化失败场景处理”，而上下文没有说明具体改动：

- **编辑模式：** 保留到证据支持的粒度，例如改成“调整失败场景的处理”，不要猜实现。
- **审阅模式：** 指出“这里需要补充实际行为”，不要替作者编一个行为。

同样，不要为了让注释解释“为什么”而猜设计理由。

### 2. 保留程序语义载体

除非用户明确要求，不修改：

- 类名、方法名、变量名、常量名
- API、协议、数据库字段名
- CLI 命令、配置键、环境变量
- 日志原文、错误码、异常类型
- 路径、URL、版本号
- 用户界面固定文案、本地化 key
- 测试断言中的固定字符串

可以修改这些内容周围的说明文字。

### 3. 保留正式术语和项目术语

不要根据词形判断一个词是不是 AI 生造词。`XX性`、`XX化`、`XX度`、`XX链路` 等后缀本身不是证据。

例如这些词在合适语境中都可能准确：

- 幂等性、原子性、一致性、可用性、可观测性
- 序列化、初始化、结构化、标准化、虚拟化
- 复杂度、相似度、置信度
- 调用链路、数据链路、全链路压测
- 回源、透传、背压、熔断、扇出

术语判断优先级：

1. 用户明确指定的术语
2. 项目 glossary、`AGENTS.md`、`CONTRIBUTING.md`、ADR / RFC 中的既有术语
3. 代码和接口中稳定使用的领域术语
4. 行业通用技术术语
5. 普通中文

只有同时满足以下条件时，才优先拆掉一个“概念词”：

- 项目和行业中没有稳定含义
- 这个词没有增加技术精度
- 它只是把本可直接描述的动作或状态包装成一个新概念

例如 `失败` 是普通词。`失败态承接` 如果项目没有定义，而且上下文只是说“失败时显示错误”，可以直接写实际行为。若实际行为未知，不要猜。

### 4. 保留 modality 和 uncertainty

不得改变要求强度、发生概率或确定性。

重点保护：

- 必须 / 应该 / 可以 / 可能 / 不得
- MUST / SHOULD / MAY
- always / sometimes / never
- 通常 / 偶尔 / 仅在 / 至少 / 至多

不能把“可能失败”改成“会失败”，也不能把 SHOULD 改成 MUST。

### 5. 最小编辑

只改真正影响理解、准确性或明显带有 AI 腔的文字。

- 已经准确、自然的段落不要为了统一风格重写。
- 不顺手重排无关章节。
- 不为了减少重复而给同一技术对象换名称。
- 保留原有 Markdown、代码块、链接和项目模板，除非结构本身影响阅读。
- 修改范围应尽量与当前任务和 diff 对齐，减少无意义的 review noise。

## Document-type rules

先判断文字属于哪一类，再使用对应标准。

### README、设计文档、ADR、RFC

优先写清：行为、约束、接口关系、取舍、决策和有来源的理由。

自由结构段落可以先写主要结论，再补原因和细节。已有 ADR / RFC 模板、规范章节顺序或团队约定不要为了“主结论提前”而重排。

### JavaDoc、docstring、API 文档

这里经常需要说明 **what / contract**，不要套用“注释只解释 why”。根据接口需要记录：

- 参数和返回值
- 异常和错误条件
- nullability
- units / format
- side effects
- thread-safety / concurrency
- blocking behavior
- ordering
- lifecycle
- 前置条件和边界条件

代码或类型系统已经完整表达的信息可以省略；公开契约需要明确时应保留。

### 行内注释和块注释

优先记录代码本身难以直接看出的信息：

- 设计原因，但必须有依据
- 不变量和边界条件
- 外部系统限制
- 兼容性约束
- concurrency / ordering 要求
- 临时 workaround 及其适用条件
- 容易被“顺手简化”但不能改的反直觉逻辑

不要逐行翻译代码：

```java
// 获取用户
User user = userService.getUser(id);
```

代码已经表达清楚时，删除这类注释通常比改写更好。

长期存在的代码注释不要记录“这次新增了什么”这类纯变更历史。若历史原因本身仍是当前实现的约束，可以写当前约束及必要背景。

TODO 应该说明剩余工作或解除临时方案的条件；没有依据时不要编日期、负责人或原因。

### PR、commit、changelog、migration guide

这些内容本来就是 change-oriented。可以直接写：新增、删除、替换、兼容性变化、迁移步骤和影响范围。

不要把“不要在长期代码注释里记录变更历史”误用到这些文档。

## Editing preferences

以下是软偏好。遇到项目规范、准确性或特殊文档结构时，以前面的硬约束为准。

### 用普通词和具体动作

能用熟悉、准确的词说明，就不要临时发明概念。

常见可疑表达包括：赋能、抓手、闭环、承接、拉齐、收口、心智、打法、组合拳、底层逻辑，以及没有实际内容的“能力 / 机制 / 体系 / 方案”。它们不是禁词；当它们隐藏实际动作时再改。

安全的改写例子：

- `对请求参数进行合法性校验` → `校验请求参数`
- `提供开关控制能力` → `支持通过开关控制`
- `对配置进行统一管理` → `统一管理配置`

这些改写没有加入新的实现事实。

像 `优化`、`增强`、`提升`、`完善`、`显著`、`高效`、`稳定`、`可靠` 这样的词需要有依据。依据可以来自代码、测试、规范、benchmark、项目约定或原文，不要求一定有数值指标。没有依据时，降到证据支持的说法。

### 少用空洞 AI 话术

如果删除后信息没有损失，就删掉模板化过渡和总结，例如：

- `值得注意的是`
- `需要指出的是`
- `综上所述`
- `总而言之`
- `Bottom Line:`
- `In short:`
- `it's worth noting`
- `importantly`
- `The simplest mental model is:`
- `Question? Answer.`

`delve`、`foster`、`leverage`、`genuinely` 等英文词也要看语境；有更直接的普通表达时优先普通表达。

### 少制造假对立

避免为了显得有洞察而写 `不是 X，而是 Y`、`This isn't about X. It's about Y.` 之类对比句。

真实存在两个方案、语义确实依赖排除关系或用户正在比较 X/Y 时，可以正常使用对比。

### 避免临时复合标签

不要把一次局部检查、布局或分支临时包装成看似正式的新名词，例如 `exact-head checks`、`editorial-row layouts`。项目已经定义的正式名称应保留。

### 句子和段落直接推进

- 使用清楚、具体的动词。
- 主体影响理解时写清主体；上下文已经明确时不用机械重复。
- 条件、结果和边界影响理解时写出来。
- 能明确主体时通常优先主动句，但不机械消灭被动句。
- 一个段落尽量围绕一个主要意思，让后一句自然接前一句。

### 列表服务于扫描性

当列表比连续段落更容易扫描时使用列表，例如：

- requirements / constraints
- options / trade-offs
- steps
- error cases
- 并列字段或行为
- 检查项

不要为了“看起来结构化”把连续解释拆成大量 bullet，也不要制造没有必要的多层嵌套。

## Project workflow

能访问仓库时，先快速确认局部语言约定：

- `AGENTS.md`
- `CONTRIBUTING.md`
- `README.md`
- `GLOSSARY.md` / `docs/glossary.md`
- 相关 ADR、RFC、API 文档
- 同目录下稳定维护的类似文档或注释

找到足够依据后开始工作，不要为了找术语遍历整个仓库。

### 编辑已有文件

1. 理解当前文字对应的代码、接口或设计上下文。
2. 只标记真正有问题的表达。
3. 用已有术语和有来源的事实改写。
4. 保持修改范围尽量小。
5. 检查是否改变了事实、契约、强弱语义或不确定性。

### 审阅模式

用户只要求 review 时，优先指出：

- 原文位置或原句
- 为什么影响理解或像 AI 生造表达
- 有依据时给出改法
- 缺少事实时明确说“需要补充实际行为 / 设计依据”，不要代写不存在的细节

不要逐字挑刺。

### 新写技术文字

从一开始就使用项目已有术语和直接表达，不要先生成一版抽象 AI 腔再“洗稿”。

## Final check

完成前确认：

- 有没有加入代码、原文和项目资料无法支持的新事实或设计理由？
- 有没有改变 MUST / SHOULD / MAY、可能性、频率、边界或其他强弱语义？
- 有没有误改标识符、字面量、API 名称或正式术语？
- 有没有因为 `XX性 / XX化 / XX度 / XX链路` 的词形而误杀正常术语？
- JavaDoc/docstring 是否保留了必要 contract？
- 行内注释是否在重复代码，或记录已经失去价值的变更历史？
- 有没有空泛黑话可以在不新增事实的前提下改成直接表达？
- 有没有模板化过渡、假对立或临时发明的概念名？
- 是否只改了真正需要改的文字？

一句话已经准确、自然、符合项目习惯时，保留它。